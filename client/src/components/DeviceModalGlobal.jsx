import React from "react";
/**
 * Componente global do modal de equipamento — versão "stub":
 * mantém o export para não quebrar imports, mas não renderiza FAB nenhum.
 * (O cadastro continua acessível pelo botão “+ Novo equipamento” da barra superior.)
 */
export default function DeviceModalGlobal() { 
  return null; 
}
