function createError(status = 500, message = "Error") {
  const err = new Error(message);
  err.status = status;
  err.statusCode = status;
  err.expose = status < 500;
  return err;
}

export default createError;
export { createError };
